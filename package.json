{
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "name": "movie-website",
  "version": "1.0.0",
  "main": "jquery.js",
  "devDependencies": {},
  "scripts": {
    "build": "nodejs build --output public"
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"json-server --watch db.json"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
